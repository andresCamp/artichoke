<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleName</key>
	<string>ArtichokeFilter</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<!-- macOS requires this on the extension bundle itself for the
	     network_extension category (OSSystemExtensionError code 9). -->
	<key>NSSystemExtensionUsageDescription</key>
	<string>Artichoke installs a network filter extension so it can monitor and block per-app data use on metered connections.</string>
	<key>NetworkExtension</key>
	<dict>
		<key>NEProviderClasses</key>
		<dict>
			<key>com.apple.networkextension.filter-data</key>
			<string>$(PRODUCT_MODULE_NAME).FilterDataProvider</string>
		</dict>
		<!-- Authorizes the provider to vend an NSXPCListener on this Mach
		     name (== App Group id). The app connects to it as the client. -->
		<key>NEMachServiceName</key>
		<string>$(TeamIdentifierPrefix)dev.serdna.artichoke</string>
	</dict>
</dict>
</plist>
